# Node.js / npm / yarn dependencies
# This folder can be regenerated by running `npm install` or `yarn install`
# and can be very large. Always ignore it.
node_modules/

# Eleventy specific files
# -----------------------

# Default Eleventy build output directory.
# - If GitHub Actions (or another CI/CD) builds your site, you SHOULD ignore this locally.
# - If you build locally and MANUALLY commit this folder to deploy (e.g., to a gh-pages branch
#   or to main branch if GitHub Pages is serving from /_site), then you should COMMENT OUT
#   or REMOVE the line below for your output directory.
#   _site/
# Example: if your output directory is "dist" or "public", change accordingly
# dist/
# public/

# Eleventy's default cache directory.
.cache/
# If you have configured a custom Eleventy cache directory, add that name here.

# Operating System generated files
# --------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnail cache files
Thumbs.db
ehthumbs.db
ehthumbs_vista.db

# Folder config file
._*

# Windows thumbnail cache files
[Dd]esktop.ini

# Windows Recycle Bin folder
$RECYCLE.BIN/

# Log files
# ---------
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Environment variables
# ---------------------
# Store sensitive information or environment-specific settings in .env files locally.
# Never commit them. Create a .env.example file to show what variables are needed.
.env
.env.local
.env.*.local
!.env.example
!.env.production.example # If you have specific example files

# Editor and IDE specific files
# -----------------------------
# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Sublime Text
*.sublime-project
*.sublime-workspace

# Atom
.atom/

# JetBrains IDEs (IntelliJ, WebStorm, etc.)
.idea/

# Other common temporary or generated files
# -----------------------------------------
# For example, if you use linters or bundlers that produce temp files
# .eslintcache
# *.gz

# Local development SSL certificates (if you generate them)
# *.pem
# *.key

# If you have a specific tutorial file you want to ignore:
# _site.desktop-tutorial
# _site/.desktop-tutorial